- to config add scale of whiteness for detection (if paper is too gray)
- if ID error, provide name of the image

- General tests on irl scanned papers

introduce autocalibration -- example paper, that has predefined answers
also calibration for default threshold -- determine best values down to .XXX